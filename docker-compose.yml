version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./downloads:/app/static/downloads  # 映射下载目录
    environment:
      FLASK_APP: main.py
      FLASK_RUN_HOST: 0.0.0.0